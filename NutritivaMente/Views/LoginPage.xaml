<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NutritivaMente.Views.LoginPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">

    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Email" Text="{Binding UserEmail, Mode=TwoWay}" />
            <Entry Placeholder="Password" Text="{Binding UserPswr, Mode=TwoWay}" />
            <Button Command="{Binding LoginWithEmailAndPasswordCommand}" Text="SignUp" />
            <Button Command="{Binding NavCommand}" Text="SignUp Page" />
        </StackLayout>
    </ContentPage.Content>

</ContentPage>