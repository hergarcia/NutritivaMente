<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NutritivaMente.Views.SignUpPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">


    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Email" Text="{Binding NewUserEmail, Mode=TwoWay}" />
            <Entry Placeholder="Password" Text="{Binding NewUserPswr, Mode=TwoWay}" />
            <Entry Placeholder="Password" Text="{Binding NewUserPswrRep, Mode=TwoWay}" />
            <Button Command="{Binding SignUpWithEmailAndPasswordCommand}" Text="Login" />
            <Button Command="{Binding NavCommand}" Text="LoginPage" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>